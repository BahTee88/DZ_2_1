public class Main {
    public static void main(String[] args) {
        Pet pet = new Pet(3,Color.BROW);
        System.out.println(" Возраст собаки " + pet.getAge() + " цвет " + pet.getColor() );
    }
}